{
  "id": "KRwjH3njrF4qRdph",
  "name": "Cron - Followup Stage Trials to Want to Join",
  "active": true,
  "isArchived": false,
  "versionId": "8a776141-c57e-46d2-9a5e-38e53062c95c",
  "activeVersionId": "8a776141-c57e-46d2-9a5e-38e53062c95c",
  "triggerCount": 2,
  "nodeCount": 3,
  "createdAt": "2026-08-29T01:19:10.356Z",
  "updatedAt": "2026-08-30T16:04:14.854Z",
  "settings": {
    "executionOrder": "v1",
    "availableInMCP": true
  },
  "tags": [],
  "parentFolderId": null,
  "scopes": [
    "execution:reveal",
    "workflow:create",
    "workflow:delete",
    "workflow:disableRedaction",
    "workflow:enableRedaction",
    "workflow:execute",
    "workflow:execute-chat",
    "workflow:export",
    "workflow:import",
    "workflow:list",
    "workflow:move",
    "workflow:publish",
    "workflow:read",
    "workflow:share",
    "workflow:unpublish",
    "workflow:unshare",
    "workflow:update"
  ],
  "canExecute": true,
  "connections": {
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Code",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook": {
      "main": [
        [
          {
            "node": "Code",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "nodes": [
    {
      "id": "00e025ba-8bff-4238-87a3-c81c12d6aee4",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 */2 * * *"
            }
          ]
        }
      }
    },
    {
      "id": "292766fd-6a17-4daf-b6f9-8983615cdb45",
      "name": "Code",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        224,
        96
      ],
      "parameters": {
        "jsCode": "const chatwootToken = 'nuwRKpG2bBAQBpRFznfvrMpT';\nconst chatwootBase = 'https://project1-chatwoot.efebpb.easypanel.host/api/v1/accounts/1';\n\nfunction detectLanguage(text, history = []) {\n  if (!text) text = '';\n  const clean = text.toLowerCase();\n\n  const spWords = new Set(['hola','buenos','buenas','dias','tardes','noches','gracias','quiero','prueba','planes','precio','precios','cuanto','como','donde','amigo','por','favor','ayuda','cuenta','dispositivo','dispositivos','informacion','servicio','saludos','pago','tarjeta','movil','bolivares','mes','meses','ano','listo','que','cual','cuando','tengo','tienen','estoy','puedo','el','la','los','las','un','una','unos','unas','de','del','en','con','para','me','te','se','nos','gusto','mucho','excelente','bueno','bien']);\n  const enWords = new Set(['hello','hi','hey','good','morning','afternoon','evening','thanks','thank','you','want','trial','plans','price','prices','how','much','where','please','help','account','device','devices','information','service','greetings','payment','card','month','months','year','ready','what','which','when','have','there','is','are','can','could','the','was','were','amazing','great','sports','channels','awesome','liked','loved','it','this','that','for','with','and','but','or','i','my','we','our']);\n\n  const words = clean.replace(/[^a-záéíóúñ\\s]/gi, ' ').split(/\\s+/).filter(Boolean);\n  let spCount = 0;\n  let enCount = 0;\n  for (const w of words) {\n    if (spWords.has(w)) spCount++;\n    if (enWords.has(w)) enCount++;\n  }\n  if (/[áéíóúñ¿¡]/.test(clean)) spCount += 2;\n\n  if (spCount > enCount) return 'es';\n  if (enCount > spCount) return 'en';\n\n  // Check history if tie\n  for (let i = history.length - 1; i >= 0; i--) {\n    const pClean = (history[i] || '').toLowerCase();\n    const pWords = pClean.replace(/[^a-záéíóúñ\\s]/gi, ' ').split(/\\s+/).filter(Boolean);\n    let pSp = 0, pEn = 0;\n    for (const w of pWords) {\n      if (spWords.has(w)) pSp++;\n      if (enWords.has(w)) pEn++;\n    }\n    if (/[áéíóúñ¿¡]/.test(pClean)) pSp += 2;\n    if (pSp > pEn) return 'es';\n    if (pEn > pSp) return 'en';\n  }\n\n  return 'es';\n}\n\nconst results = [];\n\n// 1. Fetch conversations with stage-trials\nlet stageTrialsConvs = [];\ntry {\n  const respTrials = await this.helpers.httpRequest({\n    method: 'GET',\n    url: `${chatwootBase}/conversations?labels[]=stage-trials`,\n    headers: { 'api_access_token': chatwootToken },\n    json: true\n  });\n  stageTrialsConvs = respTrials.data?.payload || [];\n} catch (e) {}\n\n// 2. Fetch conversations with stage-want-to-join\nlet stageWantToJoinConvs = [];\ntry {\n  const respWant = await this.helpers.httpRequest({\n    method: 'GET',\n    url: `${chatwootBase}/conversations?labels[]=stage-want-to-join`,\n    headers: { 'api_access_token': chatwootToken },\n    json: true\n  });\n  stageWantToJoinConvs = respWant.data?.payload || [];\n} catch (e) {}\n\nconst now = Date.now();\n\n// Process stage-trials conversations (20-hour elapsed check)\nfor (const conv of stageTrialsConvs) {\n  const convId = conv.id;\n  const currentLabels = conv.labels || [];\n  const customAttrs = conv.custom_attributes || {};\n\n  let msgs = [];\n  try {\n    const msgResp = await this.helpers.httpRequest({\n      method: 'GET',\n      url: `${chatwootBase}/conversations/${convId}/messages`,\n      headers: { 'api_access_token': chatwootToken },\n      json: true\n    });\n    msgs = msgResp.payload || [];\n  } catch (e) {\n    continue;\n  }\n\n  const customerMsgs = msgs.filter(m => (m.message_type === 0 || m.message_type === 'incoming') && m.private !== true);\n\n  let lastCustomerTimeMs = 0;\n  let lastCustomerContent = '';\n  const customerHistory = [];\n\n  if (customerMsgs.length > 0) {\n    const lastMsg = customerMsgs[customerMsgs.length - 1];\n    lastCustomerTimeMs = (lastMsg.created_at || lastMsg.created_at_epoch) * 1000;\n    lastCustomerContent = lastMsg.content || '';\n    for (const m of customerMsgs) {\n      if (m.content) customerHistory.push(m.content);\n    }\n  } else {\n    lastCustomerTimeMs = (conv.created_at || conv.last_activity_at) * 1000;\n  }\n\n  const elapsedHours = (now - lastCustomerTimeMs) / (1000 * 3600);\n\n  if (elapsedHours >= 20) {\n    // Transition label: remove stage-trials, add stage-want-to-join\n    const updatedLabels = currentLabels.filter(l => l !== 'stage-trials');\n    if (!updatedLabels.includes('stage-want-to-join')) {\n      updatedLabels.push('stage-want-to-join');\n    }\n\n    try {\n      await this.helpers.httpRequest({\n        method: 'POST',\n        url: `${chatwootBase}/conversations/${convId}/labels`,\n        headers: {\n          'api_access_token': chatwootToken,\n          'Content-Type': 'application/json'\n        },\n        body: { labels: updatedLabels },\n        json: true\n      });\n    } catch (e) {}\n\n    // Detect language of last customer message\n    const lang = detectLanguage(lastCustomerContent, customerHistory);\n    let followUpText = '';\n    if (lang === 'en') {\n      followUpText = 'Hello! 👋 We hope you enjoyed the **TotalTv** trial we provided. How did you like the service? Would you be interested in subscribing to one of our plans? 😊';\n    } else {\n      followUpText = '¡Hola! 👋 Esperamos que hayas disfrutado la prueba de **TotalTv** que te dimos. ¿Qué te ha parecido el servicio? ¿Te interesa suscribirte a alguno de nuestros planes? 😊';\n    }\n\n    // Send outgoing message\n    try {\n      await this.helpers.httpRequest({\n        method: 'POST',\n        url: `${chatwootBase}/conversations/${convId}/messages`,\n        headers: {\n          'api_access_token': chatwootToken,\n          'Content-Type': 'application/json'\n        },\n        body: {\n          content: followUpText,\n          message_type: 'outgoing',\n          private: false\n        },\n        json: true\n      });\n    } catch (e) {}\n\n    // Mark custom attribute\n    try {\n      await this.helpers.httpRequest({\n        method: 'POST',\n        url: `${chatwootBase}/conversations/${convId}/custom_attributes`,\n        headers: {\n          'api_access_token': chatwootToken,\n          'Content-Type': 'application/json'\n        },\n        body: {\n          custom_attributes: {\n            stage_want_to_join_followup_at: new Date().toISOString()\n          }\n        },\n        json: true\n      });\n    } catch (e) {}\n\n    results.push({\n      convId,\n      action: 'transitioned_and_sent',\n      elapsedHours: Math.round(elapsedHours * 10) / 10,\n      language: lang\n    });\n  } else {\n    results.push({\n      convId,\n      action: 'waiting',\n      elapsedHours: Math.round(elapsedHours * 10) / 10,\n      neededHours: 20\n    });\n  }\n}\n\n// Process stage-want-to-join conversations (newly marked, hasn't received follow-up yet)\nfor (const conv of stageWantToJoinConvs) {\n  const convId = conv.id;\n  const customAttrs = conv.custom_attributes || {};\n\n  if (customAttrs.stage_want_to_join_followup_at) {\n    continue;\n  }\n\n  let msgs = [];\n  try {\n    const msgResp = await this.helpers.httpRequest({\n      method: 'GET',\n      url: `${chatwootBase}/conversations/${convId}/messages`,\n      headers: { 'api_access_token': chatwootToken },\n      json: true\n    });\n    msgs = msgResp.payload || [];\n  } catch (e) {\n    continue;\n  }\n\n  const customerMsgs = msgs.filter(m => (m.message_type === 0 || m.message_type === 'incoming') && m.private !== true);\n  let lastCustomerContent = '';\n  const customerHistory = [];\n  if (customerMsgs.length > 0) {\n    lastCustomerContent = customerMsgs[customerMsgs.length - 1].content || '';\n    for (const m of customerMsgs) {\n      if (m.content) customerHistory.push(m.content);\n    }\n  }\n\n  const lang = detectLanguage(lastCustomerContent, customerHistory);\n  let followUpText = '';\n  if (lang === 'en') {\n    followUpText = 'Hello! 👋 We hope you enjoyed the **TotalTv** trial we provided. How did you like the service? Would you be interested in subscribing to one of our plans? 😊';\n  } else {\n    followUpText = '¡Hola! 👋 Esperamos que hayas disfrutado la prueba de **TotalTv** que te dimos. ¿Qué te ha parecido el servicio? ¿Te interesa suscribirte a alguno de nuestros planes? 😊';\n  }\n\n  try {\n    await this.helpers.httpRequest({\n      method: 'POST',\n      url: `${chatwootBase}/conversations/${convId}/messages`,\n      headers: {\n        'api_access_token': chatwootToken,\n        'Content-Type': 'application/json'\n      },\n      body: {\n        content: followUpText,\n        message_type: 'outgoing',\n        private: false\n      },\n      json: true\n    });\n  } catch (e) {}\n\n  try {\n    await this.helpers.httpRequest({\n      method: 'POST',\n      url: `${chatwootBase}/conversations/${convId}/custom_attributes`,\n      headers: {\n        'api_access_token': chatwootToken,\n        'Content-Type': 'application/json'\n      },\n      body: {\n        custom_attributes: {\n          stage_want_to_join_followup_at: new Date().toISOString()\n        }\n      },\n      json: true\n    });\n  } catch (e) {}\n\n  results.push({\n    convId,\n    action: 'newly_marked_sent',\n    language: lang\n  });\n}\n\nreturn [{\n  json: {\n    timestamp: new Date().toISOString(),\n    processed_count: results.length,\n    results\n  }\n}];"
      }
    },
    {
      "id": "d7b4605e-b949-491b-9a24-4e70de5de81e",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        192
      ],
      "parameters": {
        "path": "followup-stage-trials",
        "responseMode": "onReceived",
        "httpMethod": "POST"
      },
      "webhookId": "40445e64-1bbd-448d-8228-c56612851c83"
    }
  ],
  "nodeGroups": [],
  "activeVersion": {
    "sameAsDraft": true
  },
  "meta": {
    "aiBuilderAssisted": true,
    "builderVariant": "mcp"
  }
}